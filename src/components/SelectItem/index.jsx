import React from "react";
import "./style.css";

const SelectItem = (props) => {
  return (
    <>
      <select className="filter__dropdown"></select>
    </>
  );
};

export default SelectItem;
